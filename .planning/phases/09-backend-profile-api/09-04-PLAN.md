---
phase: 09-backend-profile-api
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/softropic/promora/security/api/ProfileResource.java
  - src/main/java/com/softropic/promora/security/api/AccountResource.java
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "GET /api/account/profile returns user profile data"
    - "PUT /api/account/email initiates email change"
    - "PUT /api/account/password updates password"
    - "PUT /api/account/phone updates phone number"
    - "PUT /api/account/address updates address fields"
    - "PUT /api/account/info updates core user info"
    - "PUT /api/account/2fa toggles 2FA"
  artifacts:
    - path: "src/main/java/com/softropic/promora/security/api/ProfileResource.java"
      provides: "Profile REST controller with correct path prefix"
      contains: "@RequestMapping(\"/api/account\")"
    - path: "src/main/java/com/softropic/promora/security/api/AccountResource.java"
      provides: "Legacy account controller without profile endpoints"
      min_lines: 150
  key_links:
    - from: "ProfileResource.java"
      to: "UserProfileService"
      via: "@Autowired injection"
      pattern: "UserProfileService"
    - from: "ProfileResource.java"
      to: "AccountManagementFacade"
      via: "@Autowired injection"
      pattern: "AccountManagementFacade"
---

<objective>
Fix API endpoint path prefix gap: Move profile endpoints from AccountResource to new ProfileResource with correct @RequestMapping("/api/account").

Purpose: ROADMAP specifies endpoints at /api/account/* but current implementation has them at /v1/account/api/account/* due to class-level @RequestMapping conflict.

Output: ProfileResource.java with 7 profile endpoints, AccountResource.java with profile endpoints removed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-backend-profile-api/09-VERIFICATION.md

# Source files to modify
@src/main/java/com/softropic/promora/security/api/AccountResource.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProfileResource with correct path mapping</name>
  <files>src/main/java/com/softropic/promora/security/api/ProfileResource.java</files>
  <action>
Create new ProfileResource.java in the same package as AccountResource with:

1. Class annotations:
   - @RestController
   - @RequestMapping("/api/account")  <-- This is the KEY fix

2. Autowired dependencies (copy from AccountResource):
   - UserService userService
   - UserMapper userMapper
   - AccountManagementFacade accountManagementFacade
   - UserProfileService userProfileService

3. Move these 7 endpoints from AccountResource (lines 182-298):
   - GET /profile (getProfile)
   - PUT /email (updateEmail)
   - PUT /password (updatePassword)
   - PUT /phone (updatePhone)
   - PUT /address (updateAddress)
   - PUT /info (updateInfo)
   - PUT /2fa (toggle2fa)

4. Update @GetMapping/@PutMapping paths to be relative:
   - @GetMapping("/api/account/profile") becomes @GetMapping(value = "/profile", produces = MediaType.APPLICATION_JSON_VALUE)
   - @PutMapping("/api/account/email") becomes @PutMapping(value = "/email", consumes = MediaType.APPLICATION_JSON_VALUE)
   - etc.

5. Copy all necessary imports from AccountResource

Keep all method implementations identical - only the path structure changes.
  </action>
  <verify>File exists at src/main/java/com/softropic/promora/security/api/ProfileResource.java and contains @RequestMapping("/api/account")</verify>
  <done>ProfileResource.java exists with 7 endpoints at relative paths (/profile, /email, /password, /phone, /address, /info, /2fa)</done>
</task>

<task type="auto">
  <name>Task 2: Remove profile endpoints from AccountResource</name>
  <files>src/main/java/com/softropic/promora/security/api/AccountResource.java</files>
  <action>
Remove the profile management section from AccountResource.java:

1. Delete lines 182-298 (the entire "Profile Management Endpoints" section):
   - The comment block "// =========================================================================..."
   - getProfile() method
   - updateEmail() method
   - updatePassword() method
   - updatePhone() method
   - updateAddress() method
   - updateInfo() method
   - toggle2fa() method

2. Remove unused imports that are now only in ProfileResource:
   - AddressDto
   - ChangePasswordRequestDto
   - ChangePhoneDto
   - Toggle2faDto
   - UpdateUserInfoDto
   - Address (domain entity)
   - UserProfileService

3. Remove the @Autowired UserProfileService field (line 70-71)

Keep all existing legacy endpoints (/register, /activate, /authenticate, /, /reset_password/*, /change_email, /ping).
  </action>
  <verify>AccountResource.java no longer contains any @GetMapping or @PutMapping with "/api/account" in the path</verify>
  <done>AccountResource.java contains only legacy /v1/account/* endpoints, profile endpoints fully removed</done>
</task>

<task type="auto">
  <name>Task 3: Verify application compiles and endpoints resolve correctly</name>
  <files></files>
  <action>
Run Maven compile to verify both controllers compile correctly:

```bash
cd /Users/mokwen/dev/gitrepos/bluegithub/promora && mvn compile -q
```

If compile fails, fix any missing imports or typos.

Then verify the endpoint paths are registered by checking the Spring context would map:
- /api/account/profile -> ProfileResource.getProfile()
- /v1/account/ -> AccountResource.getAccount()

(Full runtime test is deferred to human verification)
  </action>
  <verify>mvn compile succeeds with exit code 0</verify>
  <done>Application compiles without errors, both controllers have correct path mappings</done>
</task>

</tasks>

<verification>
1. ProfileResource.java exists with @RequestMapping("/api/account")
2. ProfileResource has 7 endpoints with relative paths: /profile, /email, /password, /phone, /address, /info, /2fa
3. AccountResource.java no longer contains profile endpoints
4. mvn compile succeeds
5. No duplicate endpoint paths between controllers
</verification>

<success_criteria>
- All 7 profile endpoints now resolve to /api/account/* as specified in ROADMAP
- AccountResource retains legacy /v1/account/* endpoints
- Application compiles successfully
- Gap from VERIFICATION.md is closed
</success_criteria>

<output>
After completion, create `.planning/phases/09-backend-profile-api/09-04-SUMMARY.md`
</output>
