---
phase: 09-backend-profile-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/softropic/promora/security/api/dto/ChangePasswordRequestDto.java
  - src/main/java/com/softropic/promora/security/api/dto/ChangePhoneDto.java
  - src/main/java/com/softropic/promora/security/api/dto/AddressDto.java
  - src/main/java/com/softropic/promora/security/api/dto/UpdateUserInfoDto.java
  - src/main/java/com/softropic/promora/security/api/dto/Toggle2faDto.java
autonomous: true

must_haves:
  truths:
    - "All profile update operations have typed request DTOs"
    - "Each DTO validates its fields using Jakarta Bean Validation"
    - "DTOs follow existing project patterns (class-based with getters/setters)"
  artifacts:
    - path: "src/main/java/com/softropic/promora/security/api/dto/ChangePasswordRequestDto.java"
      provides: "Password change request validation"
      contains: "currentPassword"
    - path: "src/main/java/com/softropic/promora/security/api/dto/ChangePhoneDto.java"
      provides: "Phone update request validation"
      contains: "@CamPhone"
    - path: "src/main/java/com/softropic/promora/security/api/dto/AddressDto.java"
      provides: "Address update request validation"
      contains: "addressLine1"
    - path: "src/main/java/com/softropic/promora/security/api/dto/UpdateUserInfoDto.java"
      provides: "User info update request validation"
      contains: "firstName"
    - path: "src/main/java/com/softropic/promora/security/api/dto/Toggle2faDto.java"
      provides: "2FA toggle request validation"
      contains: "enabled"
  key_links:
    - from: "All DTOs"
      to: "jakarta.validation.constraints"
      via: "validation annotations"
      pattern: "@NotNull|@Size|@CamPhone"
---

<objective>
Create request DTOs for all profile update endpoints.

Purpose: Define type-safe, validated request objects for password change, phone update, address update, user info update, and 2FA toggle operations.

Output: 5 new DTO classes in `security/api/dto/` package with proper Jakarta Bean Validation annotations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-backend-profile-api/09-RESEARCH.md

Key patterns from existing code:
- ChangeEmailDto uses class-based DTO with getters/setters (not records)
- KeyAndPasswordDto uses record syntax
- Validation: @NotNull, @Size, @Email, @CamPhone annotations
- Password: min 5, max 100 (from UserDto.PASSWORD_MIN_LENGTH/MAX_LENGTH)
- Address fields from Address.java entity
- Gender enum from com.softropic.promora.common.Gender
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create password and phone DTOs</name>
  <files>
    src/main/java/com/softropic/promora/security/api/dto/ChangePasswordRequestDto.java
    src/main/java/com/softropic/promora/security/api/dto/ChangePhoneDto.java
  </files>
  <action>
Create the dto package under security/api/ if it doesn't exist.

**ChangePasswordRequestDto.java:**
- Class-based DTO (not record, following ChangeEmailDto pattern)
- Fields:
  - `currentPassword`: @NotNull @Size(min=5, max=100)
  - `newPassword`: @NotNull @Size(min=5, max=100)
- Include no-arg constructor, all-args constructor, getters, and setters
- Package: com.softropic.promora.security.api.dto

**ChangePhoneDto.java:**
- Class-based DTO
- Fields:
  - `phone`: @NotNull @CamPhone (from com.softropic.promora.common.validation.CamPhone)
- Include no-arg constructor, all-args constructor, getters, and setters
- Package: com.softropic.promora.security.api.dto
  </action>
  <verify>
Files compile: `cd /Users/mokwen/dev/gitrepos/bluegithub/promora && ./mvnw compile -q`
  </verify>
  <done>
ChangePasswordRequestDto.java exists with currentPassword and newPassword fields with validation.
ChangePhoneDto.java exists with phone field and @CamPhone validation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create address, user info, and 2FA DTOs</name>
  <files>
    src/main/java/com/softropic/promora/security/api/dto/AddressDto.java
    src/main/java/com/softropic/promora/security/api/dto/UpdateUserInfoDto.java
    src/main/java/com/softropic/promora/security/api/dto/Toggle2faDto.java
  </files>
  <action>
**AddressDto.java:**
- Class-based DTO
- Fields (matching Address.java entity constraints):
  - `name`: @NotNull @Size(min=4, max=50) - identifies address (e.g., "HOME", "WORK")
  - `companyName`: @Size(max=250) - optional
  - `addressLine1`: @NotNull @Size(max=250)
  - `addressLine2`: @Size(max=250) - optional
  - `addressLine3`: @Size(max=250) - optional
  - `city`: @NotNull @Size(max=50)
  - `stateProvince`: @NotNull @Size(max=50)
  - `postalCode`: @NotNull @Size(max=25)
  - `country`: @NotNull @Size(max=25)
- Include no-arg constructor, getters, and setters
- Package: com.softropic.promora.security.api.dto

**UpdateUserInfoDto.java:**
- Class-based DTO
- Fields (matching UserProfileService.updateUserInformation params):
  - `firstName`: @Size(max=50) - optional
  - `lastName`: @Size(max=50) - optional
  - `langKey`: @Size(min=2, max=5) - optional
  - `nationalId`: @Size(min=5, max=50) - optional
  - `gender`: Gender enum (from com.softropic.promora.common.Gender) - optional
  - `title`: @Size(max=25) - optional
- Include no-arg constructor, getters, and setters
- Package: com.softropic.promora.security.api.dto

**Toggle2faDto.java:**
- Class-based DTO
- Fields:
  - `enabled`: @NotNull boolean - the desired 2FA state
  - `password`: @NotNull @Size(min=5, max=100) - required for verification
- Include no-arg constructor, all-args constructor, getters, and setters
- Package: com.softropic.promora.security.api.dto
  </action>
  <verify>
Files compile: `cd /Users/mokwen/dev/gitrepos/bluegithub/promora && ./mvnw compile -q`
  </verify>
  <done>
AddressDto.java exists with all address fields matching Address entity.
UpdateUserInfoDto.java exists with firstName, lastName, langKey, nationalId, gender, title fields.
Toggle2faDto.java exists with enabled boolean and password for verification.
  </done>
</task>

</tasks>

<verification>
All DTOs compile without errors:
```bash
cd /Users/mokwen/dev/gitrepos/bluegithub/promora && ./mvnw compile -q
```

DTOs exist in correct package:
```bash
ls -la src/main/java/com/softropic/promora/security/api/dto/
```

Expected: 5 DTO files with proper validation annotations.
</verification>

<success_criteria>
- 5 new DTO classes created in security/api/dto/ package
- All DTOs have proper Jakarta Bean Validation annotations
- All DTOs compile successfully with the project
- DTOs follow existing class-based pattern (not records) matching ChangeEmailDto
</success_criteria>

<output>
After completion, create `.planning/phases/09-backend-profile-api/09-01-SUMMARY.md`
</output>
