---
phase: 08-session-initialization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/pages/auth/LoginPage.vue
  - src/frontend/src/pages/auth/OtpPage.vue
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Session monitoring starts after successful login (no OTP)"
    - "Session monitoring starts after successful OTP verification"
    - "Session warning dialog appears 2 minutes before session expiry"
  artifacts:
    - path: "src/frontend/src/pages/auth/LoginPage.vue"
      provides: "initSession() call after successful login"
      contains: "initSession()"
    - path: "src/frontend/src/pages/auth/OtpPage.vue"
      provides: "initSession() call after successful OTP"
      contains: "initSession()"
  key_links:
    - from: "src/frontend/src/pages/auth/LoginPage.vue"
      to: "src/composables/useSession.js"
      via: "import and call initSession()"
      pattern: "useSession.*initSession"
    - from: "src/frontend/src/pages/auth/OtpPage.vue"
      to: "src/composables/useSession.js"
      via: "import and call initSession()"
      pattern: "useSession.*initSession"
---

<objective>
Wire session monitoring to start after successful authentication.

Purpose: Close the integration gap identified in v1-MILESTONE-AUDIT.md where the session monitoring system is fully implemented but never activated. Users currently won't see the 2-minute warning before session expiry because initSession() is never called.

Output: Modified LoginPage.vue and OtpPage.vue that initialize session monitoring after successful authentication.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1-MILESTONE-AUDIT.md

# Source files to modify
@src/frontend/src/pages/auth/LoginPage.vue
@src/frontend/src/pages/auth/OtpPage.vue

# Reference for useSession API
@src/frontend/src/composables/useSession.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire initSession in LoginPage</name>
  <files>src/frontend/src/pages/auth/LoginPage.vue</files>
  <action>
    1. Import useSession composable:
       - Add to existing imports: `import { useSession } from 'src/composables/useSession';`

    2. Destructure initSession from useSession:
       - Add near top of script setup: `const { initSession } = useSession();`

    3. Call initSession() after successful login (non-OTP path):
       - In handleLogin function, after the else block for successful login (around line 139-140)
       - Add initSession() call BEFORE router.push
       - The code should be:
         ```javascript
         } else {
           // Login successful, initialize session monitoring and redirect
           initSession();
           router.push(route.query.redirect || '/dashboard');
         }
         ```

    Note: Do NOT call initSession() in the OTP path (check.otp response) - that will be handled by OtpPage after OTP verification.
  </action>
  <verify>
    - File contains `import { useSession }`
    - File contains `const { initSession } = useSession()`
    - File contains `initSession()` call before router.push in success path
    - ESLint passes: `cd src/frontend && npm run lint -- --no-fix src/pages/auth/LoginPage.vue`
  </verify>
  <done>LoginPage.vue calls initSession() after successful direct login (no OTP required)</done>
</task>

<task type="auto">
  <name>Task 2: Wire initSession in OtpPage</name>
  <files>src/frontend/src/pages/auth/OtpPage.vue</files>
  <action>
    1. Import useSession composable:
       - Add to existing imports: `import { useSession } from 'src/composables/useSession';`

    2. Destructure initSession from useSession:
       - Add near top of script setup: `const { initSession } = useSession();`

    3. Call initSession() after successful OTP verification:
       - In handleSubmit function, after authApi.verifyOtp succeeds (around line 168-169)
       - Add initSession() call BEFORE router.push
       - The code should be:
         ```javascript
         await authApi.verifyOtp(loginInfoId.value, otp);
         initSession();
         router.push(redirectPath.value);
         ```
  </action>
  <verify>
    - File contains `import { useSession }`
    - File contains `const { initSession } = useSession()`
    - File contains `initSession()` call before router.push after verifyOtp
    - ESLint passes: `cd src/frontend && npm run lint -- --no-fix src/pages/auth/OtpPage.vue`
  </verify>
  <done>OtpPage.vue calls initSession() after successful OTP verification</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Static verification:**
   - Both files import useSession from correct path
   - Both files call initSession() before navigation
   - No lint errors in modified files

2. **Pattern verification:**
   ```bash
   # Verify initSession is called in both auth success paths
   grep -n "initSession()" src/frontend/src/pages/auth/LoginPage.vue
   grep -n "initSession()" src/frontend/src/pages/auth/OtpPage.vue
   ```

3. **Integration check:**
   - useSession composable exports initSession (already verified in audit)
   - sessionManager.js startSessionMonitoring exists (already verified in audit)
</verification>

<success_criteria>
1. LoginPage.vue imports useSession and calls initSession() after successful login (non-OTP path)
2. OtpPage.vue imports useSession and calls initSession() after successful OTP verification
3. Both files pass ESLint
4. Gap from v1-MILESTONE-AUDIT.md is closed
</success_criteria>

<output>
After completion, create `.planning/phases/08-session-initialization/08-01-SUMMARY.md`
</output>
