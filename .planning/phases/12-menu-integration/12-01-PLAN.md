---
phase: 12-menu-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/layouts/MainLayout.vue
autonomous: true

must_haves:
  truths:
    - "User can see Profile menu item in navigation drawer when authenticated"
    - "Clicking Profile menu item navigates to /profile page"
    - "Profile route has requiresAuth: true meta flag"
  artifacts:
    - path: "src/frontend/src/layouts/MainLayout.vue"
      provides: "Profile menu item in q-drawer"
      contains: "to=\"/profile\""
    - path: "src/frontend/src/router/routes.js"
      provides: "Profile route definition"
      contains: "requiresAuth: true"
  key_links:
    - from: "MainLayout.vue q-item"
      to: "/profile route"
      via: "to prop with router-link"
      pattern: "to=\"/profile\""
---

<objective>
Add Profile menu item to navigation drawer for authenticated users.

Purpose: Enable users to navigate to their profile page from the main menu
Output: Updated MainLayout.vue with Profile menu item
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/frontend/src/layouts/MainLayout.vue
@src/frontend/src/router/routes.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Profile menu item to navigation drawer</name>
  <files>src/frontend/src/layouts/MainLayout.vue</files>
  <action>
Add a Profile q-item to the q-drawer's q-list, after the existing Dashboard item (line 92).

Insert the following after the Dashboard q-item closing tag (after line 92):

```vue
<q-item clickable to="/profile">
  <q-item-section avatar>
    <q-icon name="person" />
  </q-item-section>
  <q-item-section>
    <q-item-label>{{ t('profile.title') }}</q-item-label>
  </q-item-section>
</q-item>
```

The `t('profile.title')` key already exists:
- en-US: "My Profile"
- fr-FR: "Mon Profil"

The profile route already exists in routes.js (path: 'profile', meta: { requiresAuth: true }).
  </action>
  <verify>
1. Run `grep -A5 'to="/profile"' src/frontend/src/layouts/MainLayout.vue` to confirm Profile q-item exists
2. Run frontend dev server and login to verify Profile menu item appears in drawer
3. Click Profile menu item and confirm navigation to /profile page
  </verify>
  <done>
- Profile menu item appears in drawer below Dashboard when user is authenticated
- Clicking Profile navigates to /profile page
- Menu item displays localized text (English: "My Profile", French: "Mon Profil")
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify route configuration</name>
  <files>src/frontend/src/router/routes.js</files>
  <action>
Verify the profile route already exists with requiresAuth: true. No changes needed to routes.js.

The route should already have:
- path: 'profile'
- name: 'profile'
- component: () => import('pages/ProfilePage.vue')
- meta: { requiresAuth: true }

This is a verification-only task. If the route is missing or incorrect, add/fix it following the pattern of the dashboard route.
  </action>
  <verify>
Run `grep -A4 "path: 'profile'" src/frontend/src/router/routes.js` to confirm:
- Route exists with path 'profile'
- Has meta.requiresAuth: true
  </verify>
  <done>
- Profile route exists in routes.js
- Route has requiresAuth: true meta flag
- Unauthenticated users redirected to login when accessing /profile
  </done>
</task>

</tasks>

<verification>
1. Profile menu item visible in drawer when authenticated
2. Profile menu item hidden when not authenticated (drawer itself is hidden)
3. Navigation to /profile works from menu click
4. Profile route protected by requiresAuth guard
5. i18n works for menu label (English/French)
</verification>

<success_criteria>
- Authenticated user sees Profile item in navigation drawer
- Clicking Profile item navigates to /profile page
- Profile route has requiresAuth: true (already present)
- Menu label displays correctly in both English and French
</success_criteria>

<output>
After completion, create `.planning/phases/12-menu-integration/12-01-SUMMARY.md`
</output>
