---
phase: 11-frontend-profile-page
plan: 04
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/frontend/src/components/profile/UpdateAddressDialog.vue
  - src/frontend/src/components/profile/UpdateInfoDialog.vue
  - src/frontend/src/components/profile/Toggle2faDialog.vue
  - src/frontend/src/pages/ProfilePage.vue
autonomous: true

must_haves:
  truths:
    - "User can open dialog to update address"
    - "User can open dialog to update core info"
    - "User can toggle 2FA with password confirmation"
    - "Address dialog validates all required fields"
    - "Info dialog allows partial updates"
  artifacts:
    - path: "src/frontend/src/components/profile/UpdateAddressDialog.vue"
      provides: "Address update dialog"
      min_lines: 120
    - path: "src/frontend/src/components/profile/UpdateInfoDialog.vue"
      provides: "User info update dialog"
      min_lines: 100
    - path: "src/frontend/src/components/profile/Toggle2faDialog.vue"
      provides: "2FA toggle dialog"
      min_lines: 60
  key_links:
    - from: "src/frontend/src/components/profile/UpdateAddressDialog.vue"
      to: "/api/account/address"
      via: "profileApi.updateAddress"
      pattern: "profileApi\\.updateAddress"
    - from: "src/frontend/src/components/profile/UpdateInfoDialog.vue"
      to: "/api/account/info"
      via: "profileApi.updateInfo"
      pattern: "profileApi\\.updateInfo"
    - from: "src/frontend/src/components/profile/Toggle2faDialog.vue"
      to: "/api/account/2fa"
      via: "profileApi.toggle2fa"
      pattern: "profileApi\\.toggle2fa"
---

<objective>
Create update dialogs for address, user info, and 2FA toggle, and wire them to ProfilePage.

Purpose: Complete the profile update functionality by enabling address, personal info, and 2FA changes.
Output: Three remaining dialog components wired to ProfilePage, completing all UPDT requirements.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-frontend-profile-page/11-RESEARCH.md
@.planning/phases/11-frontend-profile-page/11-02-SUMMARY.md

# Backend DTO definitions (for field reference)
@src/main/java/com/softropic/promora/security/api/dto/AddressDto.java
@src/main/java/com/softropic/promora/security/api/dto/UpdateUserInfoDto.java
@src/main/java/com/softropic/promora/security/api/dto/Toggle2faDto.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UpdateAddressDialog.vue</name>
  <files>src/frontend/src/components/profile/UpdateAddressDialog.vue</files>
  <action>
Create src/frontend/src/components/profile/UpdateAddressDialog.vue.

**Props:**
- modelValue: Boolean
- currentAddress: Object (optional, to pre-fill existing address)

**Emits:**
- update:modelValue
- updated

**Form fields (matching AddressDto):**
1. Address Name (required, min 4 chars) - select with options HOME, WORK, OTHER
2. Company Name (optional, max 250)
3. Address Line 1 (required, max 250)
4. Address Line 2 (optional, max 250)
5. Address Line 3 (optional, max 250)
6. City (required, max 50)
7. State/Province (required, max 50)
8. Postal Code (required, max 25)
9. Country (required, max 25)

**Template layout (use 2-column grid for some fields):**
```vue
<template>
  <q-dialog v-model="dialogVisible" persistent>
    <q-card style="min-width: 500px; max-width: 600px;">
      <q-card-section>
        <div class="text-h6">{{ $t('profile.updateAddress') }}</div>
      </q-card-section>

      <q-card-section>
        <q-form @submit.prevent="handleSubmit" class="q-gutter-md">
          <div class="row q-col-gutter-md">
            <!-- Address Name (select) -->
            <div class="col-12 col-md-6">
              <q-select
                v-model="form.name"
                :options="addressNameOptions"
                :label="$t('profile.addressName')"
                :hint="$t('profile.addressNameHint')"
                outlined
                emit-value
                map-options
                :rules="[required]"
                :error="hasFieldError('name')"
                :error-message="getFieldError('name')"
              />
            </div>

            <!-- Company Name (optional) -->
            <div class="col-12 col-md-6">
              <q-input v-model="form.companyName" :label="$t('profile.companyName')" outlined />
            </div>

            <!-- Address lines -->
            <div class="col-12">
              <q-input v-model="form.addressLine1" :label="$t('profile.addressLine1')" outlined :rules="[required]" />
            </div>
            <div class="col-12">
              <q-input v-model="form.addressLine2" :label="$t('profile.addressLine2')" outlined />
            </div>
            <div class="col-12">
              <q-input v-model="form.addressLine3" :label="$t('profile.addressLine3')" outlined />
            </div>

            <!-- City and State -->
            <div class="col-12 col-md-6">
              <q-input v-model="form.city" :label="$t('profile.city')" outlined :rules="[required]" />
            </div>
            <div class="col-12 col-md-6">
              <q-input v-model="form.stateProvince" :label="$t('profile.stateProvince')" outlined :rules="[required]" />
            </div>

            <!-- Postal Code and Country -->
            <div class="col-12 col-md-6">
              <q-input v-model="form.postalCode" :label="$t('profile.postalCode')" outlined :rules="[required]" />
            </div>
            <div class="col-12 col-md-6">
              <q-input v-model="form.country" :label="$t('profile.country')" outlined :rules="[required]" />
            </div>
          </div>

          <!-- Error banner -->
          <q-banner v-if="hasError && !isValidationError" class="bg-negative text-white" rounded>
            {{ errorMessage }}
          </q-banner>
        </q-form>
      </q-card-section>

      <q-card-actions align="right">
        <q-btn flat :label="$t('common.cancel')" @click="close" />
        <q-btn color="primary" :label="$t('common.save')" :loading="isSubmitting" @click="handleSubmit" />
      </q-card-actions>
    </q-card>
  </q-dialog>
</template>
```

**Address name options:**
```javascript
const addressNameOptions = computed(() => [
  { label: 'HOME', value: 'HOME' },
  { label: 'WORK', value: 'WORK' },
  { label: 'OTHER', value: 'OTHER' }
]);
```

**Pre-fill from currentAddress prop:**
```javascript
watch(() => props.currentAddress, (addr) => {
  if (addr) {
    form.value = { ...addr };
  }
}, { immediate: true });
```

**Submit:** Call profileApi.updateAddress(form.value)
  </action>
  <verify>File exists at src/frontend/src/components/profile/UpdateAddressDialog.vue</verify>
  <done>UpdateAddressDialog validates all address fields and calls profileApi.updateAddress</done>
</task>

<task type="auto">
  <name>Task 2: Create UpdateInfoDialog.vue</name>
  <files>src/frontend/src/components/profile/UpdateInfoDialog.vue</files>
  <action>
Create src/frontend/src/components/profile/UpdateInfoDialog.vue.

**Props:**
- modelValue: Boolean
- currentInfo: Object (firstName, lastName, nationalId, gender, title, langKey)

**Emits:**
- update:modelValue
- updated

**Form fields (matching UpdateUserInfoDto - all optional):**
1. Title (select: Mr., Mrs., Ms., Dr., Prof.)
2. First Name (max 50)
3. Last Name (max 50)
4. National ID (min 5, max 50)
5. Gender (select: MALE, FEMALE, OTHER)
6. Language (select: en, fr)

**Template:** Use 2-column grid layout similar to RegisterPage.

**Options (reuse patterns from RegisterPage):**
```javascript
const titleOptions = computed(() => [
  { label: t('auth.titleMr'), value: 'Mr.' },
  { label: t('auth.titleMrs'), value: 'Mrs.' },
  { label: t('auth.titleMs'), value: 'Ms.' },
  { label: t('auth.titleDr'), value: 'Dr.' },
  { label: t('auth.titleProf'), value: 'Prof.' }
]);

const genderOptions = computed(() => [
  { label: t('auth.male'), value: 'MALE' },
  { label: t('auth.female'), value: 'FEMALE' },
  { label: t('auth.other'), value: 'OTHER' }
]);

const languageOptions = computed(() => [
  { label: t('auth.languageEnglish'), value: 'en' },
  { label: t('auth.languageFrench'), value: 'fr' }
]);
```

**Pre-fill from currentInfo prop:**
```javascript
watch(() => props.currentInfo, (info) => {
  if (info) {
    form.value = {
      title: info.title || null,
      firstName: info.firstName || '',
      lastName: info.lastName || '',
      nationalId: info.nationalId || '',
      gender: info.gender || null,
      langKey: info.langKey || 'en'
    };
  }
}, { immediate: true });
```

**Submit:** Build object with only non-empty fields, call profileApi.updateInfo(infoData)
```javascript
async function handleSubmit() {
  const infoData = {};
  if (form.value.title) infoData.title = form.value.title;
  if (form.value.firstName) infoData.firstName = form.value.firstName;
  if (form.value.lastName) infoData.lastName = form.value.lastName;
  if (form.value.nationalId) infoData.nationalId = form.value.nationalId;
  if (form.value.gender) infoData.gender = form.value.gender;
  if (form.value.langKey) infoData.langKey = form.value.langKey;

  await profileApi.updateInfo(infoData);
  // ... success handling
}
```
  </action>
  <verify>File exists at src/frontend/src/components/profile/UpdateInfoDialog.vue</verify>
  <done>UpdateInfoDialog allows partial update of user info fields, calls profileApi.updateInfo</done>
</task>

<task type="auto">
  <name>Task 3: Create Toggle2faDialog.vue and wire remaining dialogs to ProfilePage</name>
  <files>
    src/frontend/src/components/profile/Toggle2faDialog.vue
    src/frontend/src/pages/ProfilePage.vue
  </files>
  <action>
**Part A: Create Toggle2faDialog.vue**

Create src/frontend/src/components/profile/Toggle2faDialog.vue.

**Props:**
- modelValue: Boolean
- currentEnabled: Boolean (current 2FA status)

**Emits:**
- update:modelValue
- updated

**Form fields:**
1. Password (required, min 5 chars, visibility toggle)

**Dialog content is dynamic based on currentEnabled:**
- If currently disabled (enabling): Show t('profile.confirm2faEnable')
- If currently enabled (disabling): Show t('profile.confirm2faDisable')

**Template:**
```vue
<template>
  <q-dialog v-model="dialogVisible" persistent>
    <q-card style="min-width: 400px;">
      <q-card-section>
        <div class="text-h6">{{ $t('profile.toggle2fa') }}</div>
      </q-card-section>

      <q-card-section>
        <p>{{ props.currentEnabled ? $t('profile.confirm2faDisable') : $t('profile.confirm2faEnable') }}</p>

        <q-form @submit.prevent="handleSubmit" class="q-gutter-md q-mt-md">
          <q-input
            v-model="form.password"
            :type="isPwd ? 'password' : 'text'"
            :label="$t('auth.password')"
            outlined
            lazy-rules
            :rules="[required, minLen5]"
            :error="hasFieldError('password')"
            :error-message="getFieldError('password')"
          >
            <template #append>
              <q-icon :name="isPwd ? 'visibility_off' : 'visibility'" class="cursor-pointer" @click="isPwd = !isPwd" />
            </template>
          </q-input>

          <q-banner v-if="hasError && !isValidationError" class="bg-negative text-white" rounded>
            {{ errorMessage }}
          </q-banner>
        </q-form>
      </q-card-section>

      <q-card-actions align="right">
        <q-btn flat :label="$t('common.cancel')" @click="close" />
        <q-btn
          :color="props.currentEnabled ? 'negative' : 'positive'"
          :label="props.currentEnabled ? $t('profile.disable2fa') : $t('profile.enable2fa')"
          :loading="isSubmitting"
          @click="handleSubmit"
        />
      </q-card-actions>
    </q-card>
  </q-dialog>
</template>
```

**Submit:**
```javascript
async function handleSubmit() {
  const newEnabled = !props.currentEnabled;
  await profileApi.toggle2fa(newEnabled, form.value.password);
  $q.notify({
    type: 'positive',
    message: newEnabled ? t('success.twoFactorEnabled') : t('success.twoFactorDisabled')
  });
  emit('updated');
  close();
}
```

**Part B: Wire remaining dialogs to ProfilePage.vue**

Add imports:
```javascript
import UpdateAddressDialog from 'src/components/profile/UpdateAddressDialog.vue';
import UpdateInfoDialog from 'src/components/profile/UpdateInfoDialog.vue';
import Toggle2faDialog from 'src/components/profile/Toggle2faDialog.vue';
```

Add computed for currentInfo (to pass to UpdateInfoDialog):
```javascript
const currentInfo = computed(() => {
  if (!profile.value) return null;
  return {
    title: profile.value.title,
    firstName: profile.value.firstName,
    lastName: profile.value.lastName,
    nationalId: profile.value.nationalId,
    gender: profile.value.gender,
    langKey: profile.value.langKey
  };
});
```

Add dialog components (after the three from plan 03):
```vue
<UpdateAddressDialog
  v-model="showAddressDialog"
  :current-address="profile?.address"
  @updated="loadProfile"
/>
<UpdateInfoDialog
  v-model="showInfoDialog"
  :current-info="currentInfo"
  @updated="loadProfile"
/>
<Toggle2faDialog
  v-model="show2faDialog"
  :current-enabled="profile?.otpEnabled"
  @updated="loadProfile"
/>
```
  </action>
  <verify>Toggle2faDialog.vue exists AND ProfilePage.vue imports and uses all six dialogs</verify>
  <done>All six update dialogs created and wired to ProfilePage, completing PROF-01</done>
</task>

</tasks>

<verification>
1. All three dialog files exist in src/frontend/src/components/profile/
2. UpdateAddressDialog validates all required address fields
3. UpdateInfoDialog allows partial update (only sends non-empty fields)
4. Toggle2faDialog shows enable/disable based on current state
5. ProfilePage imports and mounts all six dialogs
6. Each Edit button opens corresponding dialog
7. After any successful update, profile data refreshes
</verification>

<success_criteria>
- UPDT-04: User can update address (all required fields validated)
- UPDT-05: User can update core info (partial updates allowed)
- UPDT-06: User can toggle 2FA (with password confirmation)
- PROF-01: Complete profile page with all display and update functionality
- All dialogs follow existing codebase patterns
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-profile-page/11-04-SUMMARY.md`
</output>
